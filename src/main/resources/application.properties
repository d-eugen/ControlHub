spring.application.name=controlcenter
server.port=8081

spring.datasource.url=jdbc:mysql://localhost:3306/controlcenter
spring.datasource.username=dev
spring.datasource.password=Qwerty!@345
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# This will not create the new schema because connection to jdbc:mysql://localhost:3306/controlcenter will fail
# because 'controlcenter' still does not exist. Need custom Database Initializr, but it is out of topic now.
# spring.sql.init.mode=always
# spring.sql.init.schema-locations=classpath:schema.sql

# Optional settings for JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect